/// @description  get_save_file_name( saveIndex )
/// @param  saveIndex 
// Get the file name for a save with the given index.

var saveIndex = argument0;

return "Saves\\save" + string( saveIndex );
